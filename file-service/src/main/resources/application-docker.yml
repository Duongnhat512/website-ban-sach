
spring:
  application:
    name: file-service
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://server-registry:8761/eureka
  instance:
    prefer-ip-address: true
server:
  port: 9000
